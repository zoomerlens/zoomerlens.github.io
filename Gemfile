source 'https://rubygems.org'

ruby RUBY_VERSION rescue nil

gem 'jekyll', '~> 4.3'
# webrick required for some Ruby versions to run the server
gem 'webrick', '~> 1.7'

# the repo's _config.yml references jekyll-theme-minimal
gem 'jekyll-theme-minimal'
